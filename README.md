# Voice Phishing Detection

Detects and classifies voice phishing attacks from audio using deep learning (RawNet). The project provides scripts for training, evaluation, and feature extraction.

## Steps
1. Put your dataset in the `data/` directory.
2. Install dependencies:
